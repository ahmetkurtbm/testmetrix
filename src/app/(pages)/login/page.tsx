"use client";
import { Button } from "@/components/ui/button";
import { useRouter } from "next/navigation";

const LoginPage = () => {
  const router = useRouter();

  return (
    <div>
      <p>Login Sayfasıdır...</p>
      <Button>Naberrr</Button>
    </div>
  );
};

export default LoginPage;
